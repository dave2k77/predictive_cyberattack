# Legacy Coursework Artifacts

This directory contains the original project files from the first version of the work.

They are retained for provenance only. The active, runnable project now lives in:

- `src/predictive_cyberattack/`
- `hive/`
- `configs/`
- `docs/`

The original scripts were useful as exploratory notebooks and assignment deliverables, but the modern project treats them as historical reference rather than production pipeline code.
